[project]
name = "nflpicker"
version = "0.1.0"
description = "NFL projection, market-comparison and pick recommendation engine"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.110",
    "uvicorn[standard]>=0.27",
    "httpx>=0.27",
    "numpy>=1.24",
    "pandas>=2.0",
    "scikit-learn>=1.3",
    "scipy>=1.10",
    "pyarrow>=14.0",
    "python-dateutil>=2.8",
]

[project.optional-dependencies]
# Pillow is a dev dependency only: it draws the app icon (scripts/make_icon.py)
# and one test reads the .ico back. The app itself never imports it.
dev = ["pytest>=7.4", "ruff>=0.4", "pywebview>=5.0", "pillow>=10.0"]

[project.scripts]
nflpicker = "nflpicker.cli:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["nflpicker*"]

[tool.setuptools.package-data]
nflpicker = ["web/*"]

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
ignore = ["E501"]

[tool.pytest.ini_options]
testpaths = ["tests"]
filterwarnings = ["ignore::DeprecationWarning"]
